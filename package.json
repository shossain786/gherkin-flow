{
  "name": "gherkin-flow",
  "displayName": "Gherkin Flow",
  "description": "Run Cucumber scenarios directly from .feature files in VS Code.",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gherkinFlow.runScenario",
    "onLanguage:gherkin",
    "workspaceContains:**/*.feature"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "gherkin",
        "aliases": ["Gherkin", "Cucumber"],
        "extensions": [".feature"]
      }
    ],
    "commands": [
      {
        "command": "gherkinFlow.runScenario",
        "title": "Run Scenario (GherkinFlow)",
        "category": "GherkinFlow"
      },
      {
        "command": "gherkinFlow.runFeature",
        "title": "Run Feature File (GherkinFlow)",
        "category": "GherkinFlow"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "gherkinFlow.runScenario",
          "when": "resourceExtname == .feature",
          "group": "navigation@1"
        },
        {
          "command": "gherkinFlow.runFeature",
          "when": "resourceExtname == .feature",
          "group": "navigation@2"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/vscode": "^1.80.0",
    "@types/node": "^20.11.0",
    "@vscode/test-electron": "^2.5.0"
  }
}
